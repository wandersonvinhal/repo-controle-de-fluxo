name: Worlflow Evento
on:
  push:
    branches: ["main", "feature/*"]
jobs:
    teste:
        runs-on: ubuntu-latest
        steps:
            - name: Action de teste
              run: echo "Action executad apenas nas branches: feature/*"